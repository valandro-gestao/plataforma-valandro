Underline tab navigation for switching views within a screen (e.g. dashboard sections).

```jsx
<Tabs items={[{label:'Visão geral',value:'overview'},{label:'Fluxo de Caixa',value:'cashflow'}]} active={tab} onChange={setTab} />
```
