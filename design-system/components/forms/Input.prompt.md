Labelled text field with focus ring, error and help-text states.

```jsx
<Input label="E-mail" placeholder="voce@empresa.com" helpText="Usamos para enviar seus relatórios" />
```

Pass `error` to switch the border/ring to the negative color and show an inline message instead of `helpText`.
