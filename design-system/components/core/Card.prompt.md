Base surface for grouping content — KPI tiles, list rows, panels. Flat white, subtle border, soft shadow, generous 24px padding (never a colored left border).

```jsx
<Card title="Receita do mês" subtitle="Julho 2026">
  <span style={{fontSize:32,fontWeight:800}}>R$ 128.400</span>
</Card>
```
